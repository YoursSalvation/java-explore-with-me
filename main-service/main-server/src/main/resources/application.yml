server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://main-db:5432/ewm
    username: ewm
    password: ewm

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

stats-server:
  url: http://stats-server:9090